/*import React from 'react'

function Lily2() {
    return (
        <div className="App">
        <header className="App-header">
            <h1>Spider Lily</h1>
            <p>Spider Lily Gallery Walkthrough</p>
            <ul>
                {spiderLilyList.map((item, index) => {
                    return (
                        <li key={index}>
                            <div>
                                <img src={item.imageUrl} alt={item.title}></img>
                                <div>{item.title}</div>
                            </div>
                        </li>
                    )
                })}
            </ul>
        </header>
    </div>
    )
}

export default Lily2*/